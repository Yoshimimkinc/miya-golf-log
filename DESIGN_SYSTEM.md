# miyaGolf デザインシステム（トンマナ）

このアプリのトーン＆マナーと実装ルールを定めた文書です。
新しい画面・機能を追加するときは、必ずこの文書に従ってください。

## 0. ブランドの人格

- **キーワード**: 上質なゴルフブランド / JLPGA中継の信頼感 / 記録が楽しくなる道具
- **参考**: MALBON GOLF（ロゴ・遊び心）、JLPGA公式サイト（数字と表の見せ方）、デジタル庁デザインシステム（操作性・アクセシビリティ）
- **書き味**: 文言は短く、丁寧すぎず、プレー中でも読める長さにする

## 1. ロゴ・アイコン

- シンボルは **緑丸に白の筆記体「mg」モノグラム**（`icon.svg`）
- 背景: オフホワイト `#f7f9f7` / 円: `--main` / 字: 白、線幅27、丸キャップ
- 正式名称は **miyaGolf**。表記は「miyaGolf」（miyaは黒、Golfは緑）
- アイコン変更時は `icon.svg?v=N` のNを必ず上げる（index.html 2箇所 + site.webmanifest）

## 2. カラートークン

すべて `:root` のCSS変数で管理。**新しい色を直書きしない**こと。

| トークン | 値 | 用途 |
|---|---|---|
| `--bg` | `#f5f7f5` | 画面背景 |
| `--card` | `#fff` | カード背景 |
| `--line` | `#cdd9cd` | 罫線・枠線 |
| `--main` | `#16733a` | ブランド緑。見出しラベル・主要数字・主ボタン・表ヘッダー |
| `--main2` | `#e8f6ed` | 緑の淡背景（選択状態・バッジ） |
| `--txt` | `#142414` | 本文 |
| `--sub` | `#607560` | 補助テキスト・ラベル |
| `--warn` | `#9b3b22` | 破壊的操作（削除）・エラー |
| `--gold` | `#806000` | 現在地（進捗バーの現在ホール） |
| `--under` | `#d21f2c` | **アンダーパー（赤）** |
| `--over` | `#1f5fbf` | **オーバーパー（青）** |
| `--over2` | `#11467e` | ダボ以上の強調（濃青） |
| `--deep` | `#0d5b2c` | 濃緑。看板系（ヒーロー・ホールバー）のグラデ起点 |
| `--goldsoft` | `#ffd88a` | 濃緑地の上のゴールド文字（Par表示・ワードマークのGolf） |

### スコア色の絶対ルール（JLPGA慣習）

- **アンダー = 赤 / パー = 無色（黒 or 緑）/ オーバー = 青**
- 記号: ◎イーグル ○バーディ（赤） / △ボギー □ダボ ■トリプル+（青）
- 色だけに頼らず、必ず記号か文言を併記する（色覚多様性対応）

## 3. タイポグラフィ

- フォント: システムフォント（`-apple-system, "Noto Sans JP", sans-serif`）
- 見出し・数字・ボタンは **font-weight 900** が基本。中間の太さを乱用しない
- サイズの目安:
  - ラベル（英字キャプション）: 10〜12px / 900 / letter-spacing .1em / 大文字
  - 本文・補助: 12〜13px
  - ボタン・入力: 16px
  - カード内の主要数字: 25〜31px
  - プレー画面のホール番号: 52〜66px
- 英字ラベル（HOLE, Score Card, Stats等）＋日本語本文の組み合わせが基本トーン

## 4. 形状・余白・タップ

- 角丸: カード16px（`--r`）、ボタン14px、小部品8〜12px
- 画面パディング: 22px 18px（下は90px、ナビ分の余白）
- **タップ領域は最低44px、標準52px（`--tap`）**。プレー中に使うものは特に大きく
- 影は控えめ（`0 1px 4px rgba(0,0,0,.08)` 程度）。多用しない

## 5. コンポーネント

| 部品 | クラス | ルール |
|---|---|---|
| カード | `.card` | 白背景+線。見出しは `.k`（12px/900/サブ色） |
| 主ボタン | `.btn` | 緑地白字。1画面に1〜2個まで |
| 副ボタン | `.btn.secondary` | 白地+枠線 |
| 破壊ボタン | `.btn.warn` | 白地+赤字。**必ずconfirmを挟む** |
| 戻るボタン | `.back` | 淡緑サークル+緑シェブロン「‹」46px。ヘッダー左に固定配置 |
| ボタン列 | `.row` | 副ボタンは2列ペアにまとめ、縦積みを3段以内に抑える |
| 統計タイル | `.statRow`/`.stTile` | 3列グリッド。数字は緑900 |
| スコアカード | `.table.sc` | 緑ヘッダー・記号ルール・横スクロール禁止（画面幅に収める） |
| 成績表 | `.table.jt` | 緑ヘッダー。行タップで詳細へ |
| クイック入力 | `.qbox`/`.qgrid`/`.qbtn` | 数字1タップ入力。選択状態は緑枠+淡緑背景 |
| 横スクロール帯 | `.hscroll`/`.miniRound` | 過去一覧など。スクロールバー非表示 |
| ボトムシート | `.modal`/`.sheet` | メニューは下から。グリップ+タイトル付き |
| トースト | `.toast` | 完了通知。2秒で消える操作結果に使う |
| 注意バナー | `.statusLine`/`.bkWarn` | 黄土色系。警告・状態表示 |
| 看板 | `.hero`/`#play .holebar` | 濃緑グラデ＋フェアウェイ刈り目ストライプ。白文字＋ゴールド差し色 |
| ガラスドック | `.nav`/`.homeFixed`/`.toast` | 半透明+backdrop-blurの浮遊バー。sticky bottomに配置 |
| ゴルフボール | `.qgrid.pgrid .qbtn` | パット入力は白球＋ディンプル模様の円形ボタン |
| FWセグメント | `.fwItem` | ティーショット ←/FW/→ の3択トグル。Par3では無効化 |
| カウントチップ | `.cntChip` | OB/1ペナはタップで+1（0→3で一周）。値ありは赤系ハイライト |
| ペース行 | `.paceLine` | 進捗バー直下に「◯H ±◯・予測・ベスト更新ペース」を表示 |

## 6. インタラクション原則

1. **1画面・1タップ**: プレー中の操作はスクロールなし・最少タップで完結させる
2. 画面遷移・ホール移動には軽いアニメーション（0.2〜0.4s）と触覚（`navigator.vibrate`）
3. `prefers-reduced-motion` では必ずアニメーションを無効化する
4. 破壊的操作（削除・上書き・全置換）は **confirmで件数を明示**してから実行
5. 未入力は「-」で明示し、入力済みと区別する
6. 現在地を常に示す（進捗バー、ホール番号、画面タイトル）

### 6.1 モーションレシピ（Framer風ポリッシュ / v5.7〜）

新しい画面・ボタン・浮遊UIを追加するときは、以下のレシピをそのまま適用する。
共通イージングは **`cubic-bezier(.2,.8,.3,1)`**（スプリング風）。他のカーブを勝手に増やさない。

| レシピ | 対象 | 実装値 |
|---|---|---|
| プレスフィードバック | タップできるもの全部（`.btn` `.qbtn` `.chip` `.histCard` `.miniRound` `.statBtn` `.back` `.menuMini` `.delBtn` 等） | `transition: transform .16s cubic-bezier(.2,.8,.3,1)` ＋ `:active { transform: scale(.965) }`。グリッド内の小ボタン（`.qbtn`）だけ深めの `scale(.93)` |
| 入場スタッガー | `.screen.active` の直下子要素 | `@keyframes rise`（`translateY(10px)`＋フェード→定位置、`.34s both`）。delay は `0 / .04 / .08 / .11 / .14 / .16s`、7個目以降は一律 `.18s` |
| ガラス素材（frosted glass） | 浮遊バー・浮遊ボタン（`.nav` `.homeFixed`） | 背景 `rgba(255,255,255,.68〜.72)` ＋ `backdrop-filter: blur(12〜14px) saturate(1.5)`（`-webkit-` 併記必須）＋ 枠 `rgba(20,36,20,.07)` ＋ `border-radius:999px` ＋ 影 `0 10px 28px rgba(20,36,20,.14)` |
| ガラストースト | `.toast` | 背景 `rgba(20,36,20,.88)`＋`blur(8px)`。表示時に下から8pxスライドイン |
| 見出しの字詰め | 大きな表示文字 | 25px級の数字は `letter-spacing:-.01em`、37px以上の見出し・ホール番号は `-.02em` |
| 画面遷移 | `show(id)` | View Transitions API。クロスフェードのみ(.2s/.26s)。要素の動きは子スタッガーに一本化（二重モーション禁止）。非対応環境は即時切替 |
| モーション無効化 | 上記すべて | `@media(prefers-reduced-motion:reduce)` でアニメーション・スケールを必ず無効化する |

運用ルール:

- タップ要素を新設したら**プレスフィードバックのセレクタ一覧に必ず追加**する（1つだけ動かないボタンを作らない）
- 画面下部に固定するバー/ボタンは不透明白ではなく**ガラス素材**にする（`sticky bottom` ＋ ピル型）
- スタッガーは6要素分しか定義していない。子要素が多い画面では先頭6つに主要情報を置く

## 7. データ保護ルール

- 保存: `localStorage.miyaGolfRounds` / 下書き: `miyaGolfDraft` / バックアップ日時: `miyaGolfLastBackup`
- **アプリ更新時にこれらのキーを消してはならない**
- 破壊的変更の前にバックアップ導線を出す
- ラウンドIDは `日付_コース_レイアウト`（MDインポートと重複整理の前提）

## 8. 実装ルール

- 単一 `index.html`（CSS/JSインライン）を維持。外部依存を増やさない
- 新しい色はトークン化してから使う。SVGデータURI内のみ直書き可（コメントで対応トークンを明記）
- バージョン表記（`miyaGolf vX.Y`）はリリースごとに必ず上げる
- リリース前チェック: iPhone幅(390px)で横はみ出しなし / コンソールエラーなし / 主要フロー（ライブ入力→保存→詳細→MD出力）が通ること
